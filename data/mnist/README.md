# MNIST Dataset

This folder should contain the MNIST dataset CSV files.

Required files:

- `train.csv`
- `test.csv`

The data format is expected to be:

- Each row has 1 + 28*28 (785) cells.
- Cell 1: The correct label.
- Cells 2-785: The pixel gray scale values from 0 to 255.
